import { FormatPlacePipe } from './format-place.pipe';

describe('FormatPlacePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPlacePipe();
    expect(pipe).toBeTruthy();
  });
});
